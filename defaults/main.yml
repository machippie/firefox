# Standards: 0.1
---
# @var firefox_user:description: User to run user-specific commands
# @var firefox_user:value: firefox_user | default(homebrew_user) | default(ansible_user_id)
# firefox_user:

# @var firefox_started:description: Restart app if already running
firefox_started: True

...
